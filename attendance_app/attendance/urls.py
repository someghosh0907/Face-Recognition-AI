from django.urls import path,include
# from attendance_app.user import admin

urlpatterns = [
    # path('admin/', admin.site.urls),
    # path('user/', include('user.urls')),
    # path('attendance/', include('attendance.urls'))
]